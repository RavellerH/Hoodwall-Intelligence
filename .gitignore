# Secrets - never commit
.env
*.session
*.session-journal

# Python
venv/
__pycache__/
*.pyc
.pytest_cache/

# NOTE: data/*.json is deliberately NOT ignored.
# The JSON store in data/ IS the database - GitHub Actions commits it back
# to the repo after every run, and that commit history is the audit trail.
