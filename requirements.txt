# Runtime dependencies for the cloud pipeline.
# No LLM client and no Google API client: scoring is a local formula and
# state lives in the repo, not in Sheets.
requests==2.32.3
telethon==1.36.0
playwright==1.47.0
PyYAML==6.0.2
